<h1>Tags:</h1>
<ul>
    <% @tags.each do |tag| %>
        <li><%= link_to tag.name, tag_path(tag)%>  
            <button><%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Really Delete the tag?"}%></button>

        </li>
        <%end%>
</ul>